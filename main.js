const mainRate = document.querySelector("#main-rate");

mainRate.textContent = 5;

let count = 0

/*

    DESCRIPTION:

    With this approach we only store the rate count and current average



    THE FORMULA:

    ((currentAverage * count) + newRate) / (count + 1);


*/

